---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app "${APP}"
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    controllers:
      tubearchivist:
        containers:
          app:
            image:
              repository: bbilly1/tubearchivist
              tag: v0.5.4@sha256:d777810830e217070df43892c8b6accf6697f74dbb86a4283beb20ab91cc7d9b
            env:
              TZ: "${TZ}"
              HOST_UID: 568
              HOST_GID: 1234
              ES_URL: http://tubearchivist-elasticsearch.downloads.svc.cluster.local:9200
              REDIS_CON: redis://dragonfly-cluster.database.svc.cluster.local:6379
              TA_HOST: https://tubearchivist.${SECRET_DOMAIN}
              TA_USERNAME: admin
              DISABLE_STATIC_AUTH: "true"
            envFrom:
              - secretRef:
                  name: *app
            resources:
              requests:
                cpu: 200m
                memory: 512Mi
              limits:
                cpu: 1000m
                memory: 2Gi
            securityContext:
              fsGroup: 1234
              capabilities:
                add:
                  - CHOWN
    service:
      app:
        controller: tubearchivist
        ports:
          http:
            port: &port 8000

    route:
      app:
        hostnames: ["tubearchivist.${SECRET_DOMAIN}"]
        parentRefs:
          - name: internal
            namespace: kube-system
            sectionName: https
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/name: Tubearchivist
          gethomepage.dev/group: Downloads
          gethomepage.dev/icon: tube-archivist.png
          gethomepage.dev/app: tubearchivist-web
        rules:
          - backendRefs:
              - name: "tubearchivist-web"
                port: *port
    persistence:
      cache:
        enabled: true
        existingClaim: *app
      downloads:
        type: emptyDir
        sizeLimit: 10Gi
      youtube:
        type: nfs
        server: ${NAS_IP}
        path: ${NAS_PATH}/youtube
